<h1>New User</h1>

<%= form_for(@user) do |f| %>

	<p>
	<%= f.label "email" %>
	<%= f.text_field "email" %>
	</p>

	<p>
	<%= f.label "password" %>
	<%= f.password_field "password" %>
	</p>

	<%= f.submit "Sign Up" %>

<% end  %>
